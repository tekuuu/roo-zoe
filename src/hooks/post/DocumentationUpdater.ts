import { HookContext } from "../types"

export const updateDocumentation = async (context: HookContext, result: any): Promise<void> => {
	// Placeholder: future feature could update README or docs
	return
}
